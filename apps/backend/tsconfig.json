{
  "extends": "../../tsconfig.json",
  "include": ["src"],
  "exclude": ["node_modules", "dist"],
  "compilerOptions": {
    "outDir": "./dist",
    "baseUrl": "src",
    "rootDir": "./src",
    "paths": {
      "#root/*": ["*"],
      "#features/*": ["features/*"],
      "#lib/*": ["lib/*"],
      "#routes/*": ["routes/*"],
      "#middlewares/*": ["middlewares/*"]
    }
  }
}
